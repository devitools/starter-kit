#php

declare(strict_types=1);

use Devitools\Database\Migration\TableCreate;
use Devitools\Database\Table;

/**
 * Class {{[migration.class]}}
 */
class {{[migration.class]}} extends TableCreate
{
    /**
     * @return string
     */
    protected function table(): string
    {
        return '{{[entity.collection]}}';
    }

    /**
     * @param Table $table
     */
    protected function withStatements(Table $table): void
    {
        // REVIEW: Change the autogenerated stub
        // use ->nullable() to fields not required
        $table->string('name');

        /*
        // Add relationship example
        $table->efficientUuid('[[foreignKey]]');
        $table->foreign('[[foreignKey]]')
            ->references('uuid')
            ->on('[[foreignTable]]')
            ->onDelete('cascade');
        */
    }
}
