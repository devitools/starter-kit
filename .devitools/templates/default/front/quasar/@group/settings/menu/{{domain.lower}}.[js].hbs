import { action } from '@devitools/Security/actions'
export { domain, icon } from 'resources/views/dashboard/{{[domain.lower]}}'
// add children
// import * as example from 'resources/views/dashboard/{{[domain.lower]}}/example'

/** @type {*[]} */
export const children = [
  // action(example)
]
